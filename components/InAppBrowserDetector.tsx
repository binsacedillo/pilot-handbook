'use client';

// Component removed - website now fully supports in-app browsers
// including Messenger, Instagram, Facebook, and other in-app browsers.
// All functionality (including authentication) works within these browsers.

export function InAppBrowserDetector() {
  // No longer blocking or detecting in-app browsers
  // Return null to allow normal website operation
  return null;
}
